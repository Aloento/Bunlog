# Bunlog

A Simple Personal Blog & Frontend Framework Course Sample Work

## Requirements

- NodeJS
- PNPM

## Setup

- Run `corepack enable`

- `cd` where `package.json` is located

- Run `pnpm install`

- Run `pnpm run dev`
